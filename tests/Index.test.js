import React from 'react';
import { main } from '../app/Index';

describe('Index Form', () => {
   it('should contain the function main', () => {
       expect(main).toBe(typeOf, 'function');
    });
});